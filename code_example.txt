in any formal languages, you will really need to look at spaces inbetween, for instances
the following statements are not equivalent:

aaab;
aaab ;
 aaab;


def zero belong Natural;
def one belong Natural;
rule equiv any x :x belongs Natural->x = x;
rule sum_1 any x, y :x belong Natural,y belong Natural->x + y belong Natural;

have:one = one_from equiv (one) :one belong Natural;
have:one + one belong Natural_from sum_1 (one,one) :one belong Natural,one belong Natural;
